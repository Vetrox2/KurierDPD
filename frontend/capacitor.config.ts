import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vetrox.kurierdpd',
  appName: 'KurierDPD',
  webDir: 'dist/frontend/browser',
};

export default config;
